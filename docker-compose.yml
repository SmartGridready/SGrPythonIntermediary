######################################################################################
#
# SGr Intermediary
#
######################################################################################

services:
  # Intermediary
  sgr-intermediary:
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    restart: unless-stopped
