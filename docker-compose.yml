######################################################################################
#
# SGr Intermediary
#
#
######################################################################################

version: '3.8'
services:

  # Eclipse Mosquitto MQTT Broker
  sgr-intermediary:
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    restart: unless-stopped