######################################################################################
#
# SGr Intermediary
#
######################################################################################

services:
  # Intermediary
  sgr-intermediary:
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    restart: unless-stopped
